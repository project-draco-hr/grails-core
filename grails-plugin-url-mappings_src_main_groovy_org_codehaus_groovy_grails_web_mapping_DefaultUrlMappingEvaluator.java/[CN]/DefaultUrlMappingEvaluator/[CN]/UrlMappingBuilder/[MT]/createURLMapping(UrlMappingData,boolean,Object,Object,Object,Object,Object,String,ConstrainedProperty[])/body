{
  if (!isResponseCode) {
    return new RegexUrlMapping(urlData,controllerName,actionName,namespace,pluginName,viewName,httpMethod,constraints,sc);
  }
  return new ResponseCodeUrlMapping(urlData,controllerName,actionName,namespace,pluginName,viewName,null,sc);
}
