{
  if (isRoot())   return this;
  PersistentEntity parent=getParentEntity();
  if (parent == null) {
    return this;
  }
  while (!parent.isRoot()) {
    parent=parent.getParentEntity();
  }
  return parent;
}
