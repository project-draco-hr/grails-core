{
  try {
    if (verbose && error != null) {
      StackTraceUtils.deepSanitize(error);
      error(ERROR,"");
      printStackTrace(msg,error);
    }
 else {
      error(ERROR,msg);
    }
  }
  finally {
    postPrintMessage();
  }
}
