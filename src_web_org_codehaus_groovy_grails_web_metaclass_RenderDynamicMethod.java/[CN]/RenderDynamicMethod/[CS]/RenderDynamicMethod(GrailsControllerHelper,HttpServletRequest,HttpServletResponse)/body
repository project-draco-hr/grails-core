{
  super(METHOD_SIGNATURE,request,response);
  this.helper=helper;
  if (response instanceof GrailsHttpServletResponse)   this.response=(GrailsHttpServletResponse)response;
 else   this.response=new GrailsHttpServletResponse(response);
}
