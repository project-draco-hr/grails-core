{
  progressIndicatorActive=true;
  if (isAnsiEnabled()) {
    if (StringUtils.hasText(lastMessage)) {
      if (!lastMessage.contains(maxIndicatorString))       updateStatus(lastMessage + indicator);
    }
  }
 else {
    out.print(indicator);
  }
}
