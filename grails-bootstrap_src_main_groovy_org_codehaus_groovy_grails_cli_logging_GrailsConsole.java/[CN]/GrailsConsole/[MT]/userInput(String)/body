{
  addStatus(msg);
  lastMessage="";
  try {
    cursorMove=0;
    return reader.readLine("> ");
  }
 catch (  IOException e) {
    throw new RuntimeException("Error reading input: " + e.getMessage());
  }
}
