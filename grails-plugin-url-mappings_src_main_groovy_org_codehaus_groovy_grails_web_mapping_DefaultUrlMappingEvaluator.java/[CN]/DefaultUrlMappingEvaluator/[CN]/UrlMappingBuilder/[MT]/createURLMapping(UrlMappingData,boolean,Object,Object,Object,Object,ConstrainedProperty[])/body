{
  if (!isResponseCode) {
    return new RegexUrlMapping(urlData,controllerName,actionName,pluginName,viewName,constraints,sc);
  }
  return new ResponseCodeUrlMapping(urlData,controllerName,actionName,pluginName,viewName,null,sc);
}
