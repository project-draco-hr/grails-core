{
  if (responseCode == urlData.getResponseCode()) {
    return new DefaultUrlMappingInfo(controllerName,actionName,namespace,pluginName,viewName,parameterValues,urlData,servletContext);
  }
  return null;
}
