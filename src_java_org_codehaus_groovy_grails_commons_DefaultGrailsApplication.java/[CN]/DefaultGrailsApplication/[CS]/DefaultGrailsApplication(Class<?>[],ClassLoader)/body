{
  Assert.notNull(classes,"Constructor argument 'classes' cannot be null");
  loadedClasses.addAll(Arrays.asList(classes));
  allClasses=classes;
  cl=classLoader;
  applicationMeta=Metadata.getCurrent();
}
