{
  if (arguments == null)   throw new IllegalStateException("Parameters array must be set before retrieving Criterion");
  if (negation) {
    return Restrictions.not(createCriterion());
  }
 else {
    return createCriterion();
  }
}
