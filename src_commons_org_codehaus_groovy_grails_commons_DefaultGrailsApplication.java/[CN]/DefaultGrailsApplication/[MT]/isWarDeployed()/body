{
  Map metadata=getMetadata();
  if (metadata != null) {
    Object val=metadata.get(META_GRAILS_WAR_DEPLOYED);
    if (val != null && val.equals("true")) {
      return true;
    }
  }
  return false;
}
