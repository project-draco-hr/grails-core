{
  try {
    int len=Array.getLength(o);
    writer.array();
    for (int i=0; i < len; i++) {
      value(Array.get(o,i));
    }
    writer.endArray();
  }
 catch (  JSONException e) {
    throw new ConverterException(e);
  }
}
