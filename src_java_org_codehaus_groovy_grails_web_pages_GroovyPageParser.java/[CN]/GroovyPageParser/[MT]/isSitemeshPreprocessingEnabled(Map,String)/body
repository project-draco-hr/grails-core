{
  Object sitemeshPreprocessEnabled=config.get(CONFIG_PROPERTY_GSP_SITEMESH_PREPROCESS);
  return (sitemeshPreprocessEnabled == null || (sitemeshPreprocessEnabled instanceof Boolean && ((Boolean)sitemeshPreprocessEnabled).booleanValue()));
}
