{
  GrailsWebRequest webRequest=WebUtils.retrieveGrailsWebRequest();
  try {
    if (!redirectCalled && !isCommitted())     super.sendError(sc,msg);
  }
  finally {
    WebUtils.storeGrailsWebRequest(webRequest);
  }
}
