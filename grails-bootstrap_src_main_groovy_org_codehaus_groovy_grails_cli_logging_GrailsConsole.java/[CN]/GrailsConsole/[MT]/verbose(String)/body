{
  if (verbose) {
    if (hasNewLines(msg)) {
      out.println(msg);
      out.println();
      cursorMove=0;
    }
 else {
      out.println(msg);
      out.println();
      cursorMove=0;
    }
  }
}
