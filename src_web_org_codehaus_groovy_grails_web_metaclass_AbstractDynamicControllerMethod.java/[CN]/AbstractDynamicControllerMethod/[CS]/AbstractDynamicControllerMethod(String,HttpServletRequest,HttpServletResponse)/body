{
  super(methodName);
  this.request=request;
  if (response instanceof GrailsHttpServletResponse)   this.response=(GrailsHttpServletResponse)response;
 else {
    this.response=new GrailsHttpServletResponse(response);
  }
}
