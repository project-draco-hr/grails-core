{
  if (cfgName == null || "default".equals(cfgName))   ConvertersConfigurationHolder.setTheadLocalConverterConfiguration(XML.class,null);
 else   ConvertersConfigurationHolder.setTheadLocalConverterConfiguration(XML.class,getNamedConfig(cfgName));
}
