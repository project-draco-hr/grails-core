{
  if (theClass != null) {
    final String path=getPluginPathForClass(theClass);
    if (StringUtils.hasText(path))     return path + '/' + GrailsResourceUtils.GRAILS_APP_DIR+ "/views";
  }
  return null;
}
