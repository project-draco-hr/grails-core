{
  if (responseCode == urlData.getResponseCode()) {
    return new DefaultUrlMappingInfo(controllerName,actionName,controllerNamespace,pluginName,viewName,parameterValues,urlData,servletContext);
  }
  return null;
}
