{
  if (existingBean != null && grailsApplication != null && grailsApplication.getMainContext() instanceof AutowireCapableBeanFactory) {
    ((AutowireCapableBeanFactory)grailsApplication.getMainContext()).autowireBeanProperties(existingBean,AutowireCapableBeanFactory.AUTOWIRE_BY_NAME,false);
  }
}
