{
  sw=new StringWriter();
  out=new GSPWriter(sw);
  page();
  finalPass=true;
  scan.reset();
  page();
  InputStream in=new ByteArrayInputStream(sw.toString().getBytes());
  out=null;
  scan=null;
  return in;
}
