{
  if (id == null)   throw new IllegalArgumentException("Argument 'id' cannot be null");
  if (identityClass != null)   id=(Serializable)converter.convertValue(context,id,identityClass);
  return template.get(persistentClass,id);
}
