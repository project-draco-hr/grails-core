{
  if (this.criteria == null)   throwRuntimeException(new IllegalArgumentException("Call to [order] with propertyName [" + propertyName + "]not allowed here."));
  Order o=Order.asc(propertyName);
  this.criteria.addOrder(o);
  return o;
}
