{
  try {
    Class propType=getProperyType(clazz,propertyName);
    if (propType != null && propType.equals(type))     return true;
 else     return false;
  }
 catch (  Exception e) {
    return false;
  }
}
