{
  if (encoder == null) {
    super.write(s,off,len);
  }
 else {
    if (off == 0 && len == s.length()) {
      encodeAndPrint(s);
    }
 else {
      encodeAndPrint(s.substring(off,off + len));
    }
  }
}
