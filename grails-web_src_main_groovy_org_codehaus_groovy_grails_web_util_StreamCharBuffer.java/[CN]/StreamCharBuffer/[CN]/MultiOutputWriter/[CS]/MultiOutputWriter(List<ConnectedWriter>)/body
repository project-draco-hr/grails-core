{
  this.connectedWriters=connectedWriters;
  writers=new ArrayList<Writer>(connectedWriters.size());
  for (  ConnectedWriter connectedWriter : connectedWriters) {
    writers.add(connectedWriter.getWriter());
  }
}
