{
  if (!inStatusRun) {
    statusBegin(msg);
  }
 else {
    if (lastVerbosePrefix != null) {
      statusBegin(lastVerbosePrefix);
    }
    if (lastWasContinuous) {
      consoleOut.print(' ');
    }
    print(msg + "... ");
    inStatusRun=true;
  }
}
