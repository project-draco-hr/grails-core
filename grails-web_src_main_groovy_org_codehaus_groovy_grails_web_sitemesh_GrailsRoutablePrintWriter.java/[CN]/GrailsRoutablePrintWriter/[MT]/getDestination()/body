{
  if (destination == null) {
    try {
      destination=factory.activateDestination();
    }
 catch (    IOException e) {
      setError();
    }
    super.out=destination;
  }
  return destination;
}
