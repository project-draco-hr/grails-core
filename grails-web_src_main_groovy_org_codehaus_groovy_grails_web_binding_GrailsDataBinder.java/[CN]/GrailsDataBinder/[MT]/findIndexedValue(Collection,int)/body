{
  if (index < c.size()) {
    if (c instanceof List) {
      return ((List)c).get(index);
    }
    int j=0;
    for (    Object o : c) {
      j++;
      if (j == index)       return o;
    }
  }
  return null;
}
