{
  writer.write(OPEN_BRACKET);
  boolean first=true;
  for (  Object it : coll) {
    if (!first) {
      writer.write(COMMA);
    }
 else {
      first=false;
    }
    writeObject(writer,it,closure);
  }
  writer.write(CLOSE_BRACKET);
  return writer;
}
