{
  if (StringUtils.isBlank(name))   throw new IllegalArgumentException("Argument [name] cannot be null");
  if (factory == null)   throw new IllegalArgumentException("Argument [constraintClass] with value [" + factory + "] is not a valid constraint");
  constraints.put(name,factory);
}
