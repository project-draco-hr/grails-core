{
  try {
    usageFlag=true;
    if (csq == null)     appendNullCharSequence();
 else     CharSequences.writeCharSequence(getOut(),csq);
  }
 catch (  IOException e) {
    handleIOException(e);
  }
  return this;
}
