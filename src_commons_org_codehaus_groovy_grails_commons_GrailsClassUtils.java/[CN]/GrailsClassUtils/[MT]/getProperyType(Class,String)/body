{
  if (clazz == null || StringUtils.isBlank(propertyName))   return null;
  try {
    BeanWrapper wrapper=new BeanWrapperImpl(clazz.newInstance());
    return wrapper.getPropertyType(propertyName);
  }
 catch (  Exception e) {
    return null;
  }
}
